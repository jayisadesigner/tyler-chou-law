paths:
  static: src
  uploads: src/assets/images/uploads
  output: dist
timezone: America/Los_Angeles
markdown:
  engine: commonmark
  options: {}

collections_config:
  pages:
    path: ""
    glob:
      - "*.html"
      - "!404.html"
    icon: wysiwyg
    name: Pages
    disable_add: true
    disable_add_folder: true
    disable_delete: true

  roster:
    path: content/roster
    icon: person
    name: Roster
    singular_name: Creator
    add_options:
      - name: Add Creator
        schema: default
    schemas:
      default:
        path: .cloudcannon/schemas/roster-creator.yml
    _editables:
      content:
        italic: true
        bold: true
        link: true
    _inputs:
      handle:
        label: Handle
        comment: "YouTube/social handle, e.g. @jennyhoyos"
      slug:
        label: Slug
        comment: "URL slug — no spaces, lowercase, e.g. jennyhoyos"
      photo:
        type: image
        label: Photo
        comment: "Square photo, minimum 800x800px"
        options:
          paths:
            uploads: src/assets/images/roster
      order:
        type: number
        label: Display Order
        comment: "Lower numbers appear first in the grid"
      external_url:
        label: External URL
        comment: "If set, clicking the roster card links here instead of generating an internal page. Leave empty to generate a page."
      youtube_url:
        label: YouTube Channel URL
      seo_description:
        type: textarea
        label: SEO Description
        comment: "Appears in Google search results and social previews (150-160 characters recommended)"
      stats:
        label: Stats
        comment: "Up to 4 stats shown on the creator page"
      bio:
        type: array
        label: Bio
        comment: "Each item is one paragraph"
      featured_videos:
        type: array
        label: Featured Video IDs
        comment: "YouTube video IDs only (the part after watch?v=)"

  blog:
    path: content/blog
    icon: event_available
    name: Blog
    singular_name: Post
    _inputs:
      title:
        label: Title
      date:
        type: date
        label: Date
      description:
        type: textarea
        label: Description
      cover_image:
        type: image
        label: Cover Image
        options:
          paths:
            uploads: src/assets/images/blog
