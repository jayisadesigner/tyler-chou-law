{
  "name": "tyler-chou-law",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build:roster && npm run build:pages && npm run build:content && npm run build:blog && vite",
    "build:roster": "node scripts/build-roster.js",
    "build:blog": "node scripts/build-blog.js",
    "build:content": "node scripts/build-content.js",
    "build:sitemap": "node scripts/build-sitemap.js",
    "build:rss": "node scripts/build-rss.js",
    "build:pages": "node scripts/build-pages.js",
    "build:youtube": "node scripts/build-youtube-videos.js",
    "build": "npm run build:youtube && npm run build:roster && npm run build:pages && npm run build:content && vite build && npm run build:blog && npm run build:sitemap && npm run build:rss",
    "preview": "vite preview",
    "colors": "node scripts/generate-colors.js"
  },
  "devDependencies": {
    "dotenv": "^17.2.3",
    "js-yaml": "^4.1.1",
    "sharp": "^0.34.5",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "gsap": "^3.14.2",
    "lenis": "^1.3.16",
    "marked": "^17.0.1",
    "spring-easing": "^2.3.3"
  }
}
